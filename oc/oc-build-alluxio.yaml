apiVersion: v1
kind: BuildConfig
metadata:
  name: has-alluxio
  namespace: has
spec:
  triggers:
    - type: ConfigChange
  runPolicy: Serial
  source:
    type: Git
    git:
      uri: >-
        https://github.com/BBVA/docker-hdfs-alluxio-spark.git
    contextDir: alluxio
    sourceSecret:
      name: sshsecret
  strategy:
    type: Docker
  output:
    to:
      kind: ImageStreamTag
      name: 'has-alluxio:latest'
